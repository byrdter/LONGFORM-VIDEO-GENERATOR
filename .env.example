# Long-Form Video Generator - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Gemini API Key
# =============================================================================
# Get your API key from: https://aistudio.google.com/app/apikey
# Used for: Image generation (Imagen 3)
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# OPTIONAL: Default Voice Settings
# =============================================================================
# Default voice for narration (can be overridden per-project in segments.json)
# Run `python scripts/generate_voice.py list-voices` to see all options
# Recommended voices:
#   en-GB-RyanNeural     - British male (good for historical content)
#   en-GB-SoniaNeural    - British female
#   en-US-GuyNeural      - American male, deep
#   en-US-DavisNeural    - American male, warm
#   en-US-AriaNeural     - American female, expressive
DEFAULT_VOICE=en-GB-RyanNeural

# Speech rate adjustment: "-10%" = slower, "+10%" = faster
DEFAULT_SPEECH_RATE=+0%

# =============================================================================
# OPTIONAL: Video Settings
# =============================================================================
# Default padding (seconds) before narration starts in each segment
DEFAULT_PADDING_START=0.5

# Default padding (seconds) after narration ends in each segment
DEFAULT_PADDING_END=0.5

# Default background music volume (0.0 to 1.0)
DEFAULT_MUSIC_VOLUME=0.15

# =============================================================================
# OPTIONAL: API Rate Limiting
# =============================================================================
# Delay between Gemini API calls (seconds) to avoid rate limits
GEMINI_DELAY_SECONDS=2

# =============================================================================
# FUTURE: Additional APIs (not used in v1)
# =============================================================================
# If you want to upgrade to AI video generation later:
# KIE_API_KEY=your_kie_api_key_here

# If you want premium voice quality:
# ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
# ELEVENLABS_VOICE_ID=your_voice_id_here
